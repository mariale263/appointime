{% extends 'base.html.twig' %}

{% block title %}Subsidiary{% endblock %}

{% block body %}
    <h1>Subsidiary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subsidiary.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ subsidiary.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ subsidiary.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('subsidiary_index') }}" class"btn btn-secondary"></a>

    <a href="{{ path('subsidiary_edit', {'id': subsidiary.id}) }}">Editar</a>

    {{ include('subsidiary/_delete_form.html.twig') }}
{% endblock %}
