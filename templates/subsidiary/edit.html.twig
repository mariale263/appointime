{% extends 'base.html.twig' %}

{% block title %}Edit Subsidiary{% endblock %}

{% block body %}
    <h1>Edit Subsidiary</h1>

    {{ include('subsidiary/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('subsidiary_index') }}" class="btn btn-secondary col-md-6 mb-3">Regresar al Listado</a>

    {{ include('subsidiary/_delete_form.html.twig') }}
{% endblock %}
